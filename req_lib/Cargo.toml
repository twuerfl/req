[package]
name = "req_lib"
version = "0.1.0"
edition = "2024"

[dependencies]
# Serialization — the only external dep allowed in the qualified kernel
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Date/time
chrono = { version = "0.4", features = ["serde"] }
